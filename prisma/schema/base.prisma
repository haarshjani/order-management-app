generator client {
  provider        = "prisma-client"
  previewFeatures = ["prismaSchemaFolder"]
  output   = "../generated/prisma"
  binaryTargets = ["native"]

}

datasource db {
  provider = "sqlite"
}
